# Workflow específico para construir e publicar a imagem Docker do registrations-service no GitHub Container Registry (GHCR).
# Este workflow NÃO faz deploy em VPS via SSH; apenas constrói e publica a imagem.
# Não depende de secrets de SSH, apenas do GITHUB_TOKEN padrão para autenticação no GHCR.

name: build-registrations-service

on:
  push:
    branches: [ main ]
    paths:
      - 'packages/registrations-service/**'
      - '.github/workflows/build-registrations-service.yml'
  pull_request:
    paths:
      - 'packages/registrations-service/**'
      - '.github/workflows/build-registrations-service.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository_owner }}/registrations-service
          tags: |
            type=ref,event=branch
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ./packages/registrations-service/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
